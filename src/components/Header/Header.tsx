import React from 'react';
import styles from '@/components/header/Header.module.scss';
export function Header() {
    return (
        <header className={styles.header}>
            <h1>sekine frontend</h1>
        </header>
    );
}
