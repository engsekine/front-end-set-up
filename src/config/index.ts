export const siteInfo = {
    siteName: 'a' as string
};

export const DefaultMetaConfig = {
    iconUrl: '' as string,
    ogLocale: 'ja_JP' as string,
    formatDetection: 'content="telephone=no, email=no, address=no"' as string,
    twitterCard: 'summary_large_image' as string,
    twitterSite: 'a' as string,
    fbAppId: 'a' as string
};
